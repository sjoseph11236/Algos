// Complete the function countInversions in the editor below. It must return an integer representing the number of inversions required to sort the array.

function countInversions(arr) {
  // check if length is less than 1.
  // return arr. 
  // Set the midpoint of the arr
  // Sort the left 
  // Sort the right
  // return merge of left and right with helper function
  }

  console.log(countInversions([2,1,3,1,2]));